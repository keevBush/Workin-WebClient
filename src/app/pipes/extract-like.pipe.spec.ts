import { ExtractLikePipe } from './extract-like.pipe';

describe('ExtractLikePipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractLikePipe();
    expect(pipe).toBeTruthy();
  });
});
