import { DemandeurIdentite } from './demandeur-identite';

describe('DemandeurIdentite', () => {
  it('should create an instance', () => {
    //expect(new DemandeurIdentite()).toBeTruthy();
  });
});
